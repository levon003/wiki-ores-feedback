# Required by an unmaintained transitive dep (react-svg-tooltip, peers on React 16).
# Keeping it here means `npm install` / `npm ci` work without the --legacy-peer-deps
# flag locally and in CI. Remove once react-svg-tooltip is dropped (see DEVELOPMENT.md).
legacy-peer-deps=true
